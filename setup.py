from setuptools import setup, find_packages

setup(name='woocommapitest',
      version='1.0',
      description="Framework for API testing of woocommerce api",
      author="Trevor Harrington",
      packages=find_packages()
      )